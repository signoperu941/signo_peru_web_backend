services:
  # Backend
  signoperu-api:
    build: .
    container_name: backend-ia
    ports:
      - "8000:8000"
    restart: always
    env_file:
      - .env

  # Tunel de Cloudflare
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: tunnel-cloudflare
    # Tu comando con el protocolo http2 para saltar el firewall
    command: tunnel --protocol http2 --no-autoupdate run --token 
    restart: always
    depends_on:
      - signoperu-api